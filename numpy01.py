# arr = [0,1,2,3,4]
# print(arr)
#
# arr= []
# for i in range(5):
#     arr.append(i)
# print(arr)
import numpy as np
#  넘파이는 배열을 생성하여 준다.
a = [0,1,2,3,4]
c = np.array(a)
print(c)

#  리스트를 numpy 배열로   : np.array
#  numpy 배열을 리스트로   : list
